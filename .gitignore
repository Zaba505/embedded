.zig-cache/
zig-out/
*.o

# KiCad per-user local state, generated reports and backups
# (project, schematic, symbol library and exported SVGs are tracked)
*.kicad_prl
*-backups/
*.kicad_sch-bak
*-erc.rpt
netlist.net
fp-info-cache
